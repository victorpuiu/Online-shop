import React from "react";
import Hero from "../components/Hero";

const Home = ({ isOpen, toggle}) => {
  return (
    <>
        <Hero  />
        {/*<Content />*/}
    </>
  );
};

export default Home;
